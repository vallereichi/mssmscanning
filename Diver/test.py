"""Testing"""

import numpy as np


def test_diver(config: dict) -> None:
    """diver tests for different configurations of the Diver algorithm. The config dict must specify the parameter space and the objective function. Rest is optional"""
